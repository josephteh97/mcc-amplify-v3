================================================================================
  OLLAMA VISION MODEL BENCHMARK — FLOOR PLAN COLUMN DETECTION
  Task: Detect structural columns in architectural PDF floor plan
  Tested: 2026-04-07 to 2026-04-13  |  Rounds: 13  |  Ranked: 32 unique + 4 aliases  |  Excluded: 4
================================================================================

TEST SETUP
----------
PDF          : 2aad5b45-ce32-40a6-a00d-e5b0e67f5d08.pdf  (A2 floor plan, 1285x1273 px @ 150 DPI)
Ground truth : 67 confirmed column boxes from detections.db (original benchmark runs)
               NOTE (2026-04-10): DB GT was annotated on a DIFFERENT tile (hash mismatch).
               Corrected GT = 86 columns from human-annotated image (red circle annotations),
               extracted via CV red-pixel detection. Used for all gemma4:e4b memory/harness
               tests. CV template-matching validator achieved F1=0.94 (84/86 TP) on same tile.
Tiling       : 1280x1280 px sliding window, step 1080, 200 px overlap → 4 tiles per page
               Tile 0 (offset 0,0) covers 99% of the page — all 67 GT boxes are here
Vision size  : Tiles resized to 1024x1024 before sending to model (768x768 for minicpm-v)
IoU thresh   : 0.25 for TP matching
Conf thresh  : 0.30 minimum confidence to count a detection

SCORING RUBRIC (6 points)
--------------------------
  1. Valid JSON on >=50% of tiles
  2. At least one detection with bbox coordinates
  3. Detection count in plausible range (5-200)
  4. Precision > 0.20
  5. Recall > 0.30
  6. F1 > 0.25
Pass threshold: >=4/6 points

EXCLUDED FROM RANKING (never ran — pull failed or size prohibitive)
---------------------------------------------------------------------
  kimi-k2.5               — pull failed; tag does not exist on Ollama
  glm-ocr:98_o            — pull failed; tag does not exist on Ollama
  mistral-small3.2        — pull failed; bare tag (without :latest) does not resolve
  mistral-small3.2:latest — pull cancelled; 15 GB far exceeds 8 GB VRAM capacity

BASELINE (SEA-LION v4 4B — the original detection model)
---------------------------------------------------------
  Model    : aisingapore/Gemma-SEA-LION-v4-4B-VL:latest  (~3.3 GB)
  Metrics  : P=0.254  R=0.728  F1=0.376  (estimated from 264 human corrections)
  Notes    : 67 confirmed TPs, 197 rejected FPs, 25 missed FNs.
             High recall but 68% FP rate — the main reason YOLO was adopted in v3.


================================================================================
  COMPLETE RANKED RESULTS  (32 unique + 4 aliases — 0 passed)
================================================================================

  Note: Rank = position in this table. [N] = detail section entry ID (added in test order).
  Rank  Model                      GB     Score  Dets   P      R      F1     Status
  ──────────────────────────────────────────────────────────────────────────────────
   1    gemma4:e4b                9.6     3/6    73    0.000  0.000  0.000   FAIL  * memory injection tested + visual harness tested
   1=   gemma4:latest             9.6     3/6    73    0.000  0.000  0.000   FAIL  (= :e4b, same digest)
   2    gemma3:12b                8.1     3/6    26    0.000  0.000  0.000   FAIL  * memory injection tested
   3    qwen2.5vl:7b              6.0     3/6    12    0.000  0.000  0.000   FAIL  * memory injection tested
   4    gemma4:e2b                7.2     3/6     9    0.000  0.000  0.000   FAIL  * best JSON  * memory injection tested
   5    minicpm-v:8b              5.5     3/6     9    0.000  0.000  0.000   FAIL  * memory injection tested
   6    granite3.2-vision:2b      1.9     3/6     8    0.000  0.000  0.000   FAIL  * memory injection tested
   7    ministral-3:latest        6.0     2/6    79    0.000  0.000  0.000   FAIL  (no vision)
   8    ministral-3:3b            3.0     2/6    86    0.000  0.000  0.000   FAIL  (no vision)
   9    ministral-3:8b            6.0     2/6    85    0.000  0.000  0.000   FAIL  (no vision)
  10    gemma3:4b                 3.0     2/6     4    0.000  0.000  0.000   FAIL  * memory injection tested
  11    llama3.2-vision:11b       7.9     2/6     4    0.000  0.000  0.000   FAIL  * best arch  * memory injection tested
  12    qwen2.5vl:3b              3.2     2/6     1    0.000  0.000  0.000   FAIL  * memory injection tested
  13    qwen3-vl:4b               3.5     2/6     1    0.000  0.000  0.000   FAIL  * memory injection tested
  14    glm-ocr:latest            2.2     1/6     2    0.000  0.000  0.000   FAIL  * memory injection tested
  14=   glm-ocr:bf16              2.2     1/6     2    0.000  0.000  0.000   FAIL  (= :latest, same digest)  * memory injection tested
  15    moondream:latest          1.7     1/6     0    0.000  0.000  0.000   FAIL  * memory injection tested
  16    qwen3.5:2b                2.7     1/6     0    0.000  0.000  0.000   FAIL  (text-only)
  17    llava-phi3:3.8b           2.9     0/6     2    0.000  0.000  0.000   FAIL  (prompt echo)  * memory injection tested
  18    qwen3.5:9b                6.6     0/6     0    0.000  0.000  0.000   FAIL  (thinking)
  19    qwen3-vl:2b               1.9     0/6     0    0.000  0.000  0.000   FAIL  (thinking)
  20    qwen3-vl:8b               6.1     0/6     0    0.000  0.000  0.000   FAIL  (thinking)
  20=   qwen3-vl:latest           6.1     0/6     0    0.000  0.000  0.000   FAIL  (= :8b, same digest)
  21    qwen3.5:4b               ~3.5     0/6     0    0.000  0.000  0.000   FAIL  (thinking)  * memory injection tested
  22    qwen3.5:0.8b             ~0.7     0/6     0    0.000  0.000  0.000   FAIL  (thinking)
  23    translategemma             —      0/6     0    0.000  0.000  0.000   FAIL  (no vision)
  24    devstral                   —      0/6     0    0.000  0.000  0.000   FAIL  (no vision)
  25    deepseek-ocr:3b           2.0     0/6     0    0.000  0.000  0.000   FAIL  (OCR only)
  26    ministral-3:14b           9.1     0/6     0     —      —      —      TIMEOUT (676s)
  27    mistral-small3.1:latest  15.0     0/6     0     —      —      —      TIMEOUT (987–1686s)
  27=   mistral-small3.1:24b     15.0     0/6     0     —      —      —      TIMEOUT (= :latest, same digest)
  28    llama4:16x17b            67.0     0/6     0     —      —      —      OOM
  29    bakllava:7b               4.7     0/6     0     —      —      —      CRASH (CLIP)
  30    llava:7b                  4.7     0/6     0     —      —      —      CRASH (CLIP)
  31    llava-llama3:8b           5.5     0/6     0     —      —      —      CRASH (CLIP)
  32    glm-ocr:q8_0              1.6     0/6     0     —      —      —      CRASH (GGML)
  ──────────────────────────────────────────────────────────────────────────────────
       SEA-LION v4 4B (baseline)  3.3      —    264   0.254  0.728  0.376   BASELINE

  Models passed: 0 / 32
  (32 unique models + 4 alias tags sharing a digest with an already-ranked model)


================================================================================
  TIER 1 — Score 3/6
  Valid JSON + detections, but bounding box coordinates are wrong
================================================================================

[1] gemma4:e2b               ~7.2 GB    Score: 3/6   FAIL   * BEST JSON QUALITY  * MEMORY INJECTION TESTED
    Detections : 9 (4 columns + 5 grid lines)
    Inference  : 346s/tile (7.2 GB on 8 GB GPU — heavy CPU offload)
    JSON rate  : 100%
    Failure    : Coordinates are hallucinated — columns evenly spaced at x=200,300,400,500
                 with identical y-range [300,400]. Fabricated grid pattern, not real detections.
                 Column bboxes (50x100 px) are also too large vs real GT columns (40-50 px).
    JSON quality: EXCELLENT — best of all 27 models. Every field present, correct types,
                  plausible confidence values (0.95-0.98). Perfect structure, no repair needed.
    Detections returned:
      column    conf=0.95  bbox=[200,300,250,400]  shape=rectangle
      column    conf=0.95  bbox=[300,300,350,400]  shape=rectangle
      column    conf=0.95  bbox=[400,300,450,400]  shape=rectangle
      column    conf=0.95  bbox=[500,300,550,400]  shape=rectangle
      grid_line conf=0.98  bbox=[200,300,550,300]  shape=horizontal
      (+ 4 more grid_lines — all evenly spaced)
    NOTE       : Most promising candidate for fine-tuning. Zero-shot it hallucinates
                 plausible-but-wrong coordinates; trained on floor plan data, gemma4's
                 structured output capability could produce accurate results.
    NOTE       : Requires Ollama >=0.20.4 (fails on <=0.18.x).

    ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
    Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
      Phase        dets   TP   FP   FN    P      R     F1    elapsed
      BASELINE      111    0  111   86  0.000  0.000  0.000   130s
      ANCHOR          8    7    1   79  0.875  0.081  0.149    48s
      ADD-ONLY        0    0    0   86  0.000  0.000  0.000    90s
    Verdict: HELPS  (ANCHOR F1=0.149 vs BASELINE F1=0.000)
    Findings:
      - Baseline: 111 detections, all FP — massive over-detection hallucination.
        Model is highly active but cannot ground itself spatially.
      - ANCHOR: dramatic constraint effect — 10 seeded GT boxes reduce output to just 8
        detections (7 TP, 1 FP). Precision jumps to 0.875. Model appears to use anchor
        positions to verify only nearby candidates, suppressing bulk hallucination.
        Likely echoing 7 of the 10 provided anchors rather than finding new columns.
      - ADD-ONLY: collapses completely — 0 detections. Seeing the pool of known boxes
        makes the model conclude there is nothing left to find.
      - Conclusion: ANCHOR phase constrains gemma4:e2b's hallucination and produces high
        precision (7 TP, 0.875P) but very low recall (8%). ADD-ONLY phase is non-functional.
        Memory injection partially works for suppressing noise but not for improving coverage.

[28] gemma4:e4b              ~9.6 GB    Score: 3/6   FAIL   * MEMORY INJECTION + VISUAL HARNESS TESTED
     Detections : 73 (73 columns + 0 grid lines) — original batch run
     Inference  : 135–265s/phase (9.6 GB on 8 GB VRAM — heavy CPU offload)
     JSON rate  : 100%
     Failure    : Coordinates hallucinated — original batch run: 73 columns at tight 30 px
                  x-intervals from x=100, all packed into a single row at y=190-220.
                  Subsequent retests (3 independent runs) produced different hallucination
                  patterns each time: vertical strip at x=295, vertical strip at x=78,
                  horizontal row at y=196-226 — all fabricated uniform grids, none matching
                  the actual 86 column positions.
     NOTE       : Despite the "e4b" name (effective 4B), this model is physically 9.6 GB
                  (8B MoE weights on disk, ~4B active per token). Same digest as
                  gemma4:latest (c6eb396dbd59) — identical model.

     GT NOTE    : See TEST SETUP above — DB GT (67 boxes) was from a different tile.
                  All tests below use the corrected 86-box GT.

     ── TEXT-BASED MEMORY INJECTION TEST (2026-04-10, GT=86) ────────────────────────────
     Tile: 1024×1024 px.  All phases: temperature=0.1, num_predict=8192, keep_alive=0.
     Phases:
       BASELINE          : 36 dets  TP= 3  FP=33  FN=83  P=0.08  R=0.04  F1=0.05
       ANCHOR (10 GT→find remaining): 34 dets  TP= 3  FP=31  FN=83  P=0.09  R=0.04  F1=0.05
       ADD-ONLY round 1  :  2 dets  TP= 0  FP= 2  FN=86  P=0.00  R=0.00  F1=0.00
       ADD-ONLY round 2  :  6 dets  TP= 0  FP= 6  FN=86  P=0.00  R=0.00  F1=0.00
     Findings:
       - Baseline: hallucinates 36 boxes stacked in a vertical strip at x=295 (even y-steps).
       - Anchor phase: given 10 correct GT positions, ignores them entirely and
         generates a new unrelated hallucination pattern.
       - Add-only: after seeing accumulated pool, collapses to 2–6 random guesses (F1=0.00).
       - Memory injection makes performance WORSE in rounds 3-4.
       - Conclusion: text-based episodic memory injection has zero effect on spatial
         localisation. Model cannot use coordinate hints to guide visual search.

     ── VISUAL HARNESS TEST (2026-04-10, GT=86, 3 independent runs) ─────────────────────
     Harness design: reference images (column patches, annotated crops, reference panels)
     sent alongside the main tile in the same prompt, in 5 configurations.
     Results across 3 runs (non-deterministic at temperature=0.1):

       Phase                      Run1        Run2        Run3 (best)
       BASELINE (tile only)       F1=0.00     F1=0.071    F1=0.14   TP=11
       PATCH-REFS (8 patches+tile)F1=0.00     F1=0.00     F1=0.00
       PANEL-REF (panel+tile)     F1=0.031    F1=0.127    F1=0.00
       ANNOTATED-CROP (crop+tile) F1=0.094    F1=0.086    F1=0.00
       PANEL+ANNOT (panel+crop+tile)F1=0.00   F1=0.00     F1=0.00

     Key findings:
       1. BASELINE outperforms ALL reference-augmented phases in 2 of 3 runs —
          visual references hurt rather than help.
       2. PATCH-REFS: model switched JSON key from "bbox" to "box_2d" (format
          confusion), still hallucinated a vertical strip at x=111.
       3. PANEL-REF: 8-patch grid triggered over-detection (301 boxes, run 1) as
          model raster-scanned the floor plan matching the panel's grid spacing.
       4. ANNOTATED-CROP: correct y-levels but x evenly spaced at 140 px (synthetic).
       5. PANEL+ANNOT: collapses to 12–38 oversized boxes in a single row.

     Root cause: model pattern-matches on prompt/reference structure and invents
     coordinate grids; it does not locate the actual gray-filled column symbols.
     More visual context adds more patterns to hallucinate from.

     FINAL VERDICT: Weight-level learning required.
     YOLO fine-tuning on the 86-box GT is the correct next step.

[28=] gemma4:latest          ~9.6 GB    Score: 3/6   FAIL   (ALIAS of gemma4:e4b)
      Detections : 73
      Inference  : 220s
      Result     : Identical to gemma4:e4b — same Ollama digest (c6eb396dbd59).
                   Not re-run; result is effectively identical (73 hallucinated columns).

[29] gemma3:12b              ~8.1 GB    Score: 3/6   FAIL   * MEMORY INJECTION TESTED
     Detections : Run 1: 26  |  Run 2 (retest): 30  — hallucination CONFIRMED
     Inference  : ~229-238s (8.1 GB on 8 GB VRAM — CPU offload)
     JSON rate  : 100%
     Failure    : HALLUCINATION CONFIRMED across 2 independent runs:
                  Run 1 (batch): 26 dets — 5 cols in a single row (y=243-278), 255 px x-spacing,
                    plus 21 grid_lines at even y-intervals spanning full image width.
                  Run 2 (retest 2026-04-10): 30 dets — perfect 3x10 uniform grid:
                    x positions: 133, 253, 373 (120 px apart)
                    y positions: 133, 253, 373, 493, 613, 733, 853, 973, 1093, 1213 (120 px apart)
                    All conf=0.95, all 20x20 px boxes.
                  Both runs produce a fabricated regular grid pattern — different each time,
                  neither matching the actual floor plan geometry. Not a reliable detector.
     NOTE       : gemma3:12b and gemma3:12b-it-qat are different models (different digests).
                  Only gemma3:12b (f4031aab637d) was tested here.

     ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
     Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
       Phase        dets   TP   FP   FN    P      R     F1    elapsed
       BASELINE       36    7   29   79  0.194  0.081  0.115   153s
       ANCHOR          0    0    0   86  0.000  0.000  0.000   336s
       ADD-ONLY       10    1    9   85  0.100  0.012  0.021    34s
     Verdict: HURTS  (best=0.115 at BASELINE; ANCHOR collapses to zero output)
     Findings:
       - Baseline: gemma3:12b achieves its best score (F1=0.115) with 7 real TPs — better
         than most models tested, but surrounded by 29 FP hallucinations in a regular grid.
       - ANCHOR phase: given 10 confirmed GT positions, the model produces 0 detections.
         Anchor prompt appears to confuse the model — it over-constrains and outputs nothing.
         Total inference 336s suggests the model spent significant time but produced no JSON.
       - ADD-ONLY: partial recovery to 10 dets with 1 TP — still heavily hallucinated.
       - Conclusion: memory injection HURTS gemma3:12b. Baseline is the best configuration.
         The model's 7 TP (best of any non-anchor mode) are achieved only without memory.

[2] qwen2.5vl:7b             ~6.0 GB    Score: 3/6   FAIL
    Detections : Original: 12 after NMS  |  Retest baseline: 11  |  Memory injection: 5
    Inference  : ~160-274s/tile (partial CPU offload; 1024x1024 input)
    JSON rate  : 75% (original 4-tile run — 1 tile hit GGML tensor assertion under VRAM pressure)
    Failure    : HALLUCINATION CONFIRMED (retest 2026-04-10):
                 Baseline — 11 dets on perfect diagonal: [100,100], [200,200], ..., [1100,1100].
                 All conf=0.9 exactly, all 20x20 px boxes. Zero real detections.
    Episodic memory test (2026-04-10):
                 Injected 5 confirmed GT boxes (leftmost column, one per row) into the prompt
                 as "confirmed examples — use as visual anchors to find remaining columns."
                 Result: model returned exactly those 5 examples verbatim — bbox values are
                 pixel-perfect copies of the injected GT. It did NOT use them as anchors to
                 find new columns; it treated the examples as the complete answer.
                 Conclusion: episodic memory injection DOES NOT work for this model.
                 The model cannot do zero-shot visual grounding of 40-50 px structural symbols
                 and cannot use in-context examples to guide new detections.

[3] minicpm-v:8b             ~5.5 GB    Score: 3/6   FAIL
    Detections : Original: 9 after NMS  |  Retest: 11  |  Memory-anchor: 5  |  Memory-addonly: 4
    Inference  : ~10-57s/tile (768x768 input)
    JSON rate  : 100%
    Failure    : HALLUCINATION CONFIRMED (retest 2026-04-10):
                 Baseline — 11 dets, all at x=324-372 (center ~348, near GT column 3 x-strip),
                 y=168 to 648 at perfectly even 48px steps. Shape field copied verbatim from
                 prompt enum ("square|rectangle|round|i_beam") instead of choosing one.
                 Right x-strip, fully invented y positions.
    Episodic memory tests (2026-04-10, 768px input):
                 Memory-ANCHOR (5 GT boxes as visual anchors):
                   Returned exactly the 5 injected examples verbatim — same echo behaviour
                   as qwen2.5vl:7b. Treats examples as the answer, not as visual guidance.
                 Memory-ADDONLY ("already found, find the others"):
                   Returned 4 new detections NOT in the GT list. Box sizes 31x31 px are
                   within correct range (25-40 px). Positions (x~231/330, y~360/510) do not
                   match actual GT columns but the model genuinely searched the image.
                   This is the strongest memory-injection response seen so far — but still
                   no real GT columns found (wrong x-positions entirely).
                 Conclusion: echo behaviour identical to qwen2.5vl. Add-only variant shows
                 minicpm can attempt new detections of the right size — but cannot accurately
                 localize structural symbols without fine-tuning.
    Special    : Crashes with HTTP 500 at vision_size=512 and 1024. Only runs at 768 px —
                 resolution-specific bug in the minicpm GGUF vision encoder.

[4] granite3.2-vision:2b     ~1.9 GB    Score: 3/6   FAIL   * MEMORY INJECTION TESTED
    Detections : 8 after NMS (raw: 49 detections in main tile before NMS)
    Inference  : ~13s/tile
    JSON rate  : 100%
    Failure    : Massive over-detection — 49 raw boxes in one tile. Hallucinates column shapes
                 at every wall corner, grid intersection, and structural feature. NMS collapses
                 to 8 but none match GT boxes. Understands "find squares in floor plans" but
                 draws boxes around the wrong things.

    ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
    Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
      Phase        dets   TP   FP   FN    P      R     F1    elapsed
      BASELINE        0    0    0   86  0.000  0.000  0.000    26s
      ANCHOR        147    6  141   80  0.041  0.070  0.052    48s
      ADD-ONLY      137    0  137   86  0.000  0.000  0.000    49s
    Verdict: HELPS  (ANCHOR F1=0.052 vs BASELINE F1=0.000; technically >0.02 threshold)
    Findings:
      - Baseline: 0 detections — unusual for this model given its over-detection tendency in
        the original run. Possibly the new 1024px prompt format suppresses its output.
      - ANCHOR: seeding triggers massive over-detection (147 dets, 141 FP). Model interprets
        the provided anchor examples as permission to detect aggressively, flooding the image.
        6 TPs found but precision of 0.041 makes this practically useless.
      - ADD-ONLY: 137 dets, 0 TP — even worse than ANCHOR. Larger pool of known boxes causes
        the model to hallucinate everywhere except the actual column positions.
      - Conclusion: "HELPS" is a metric artefact — ANCHOR triggers hallucination flood.
        granite3.2-vision cannot be improved via memory injection.


================================================================================
  TIER 2 — Score 2/6
  Valid JSON + some detections, but count out of range or P/R/F1 all zero
================================================================================

[5] gemma3:4b                ~3.0 GB    Score: 2/6   FAIL
    Detections : Original: 4 after NMS  |  Retest baseline: 72  |  Memory-anchor: 23  |  Memory-addonly: 25
    Inference  : ~23-74s/tile (1024x1024 input)
    JSON rate  : 100%
    Failure    : Original run — overly conservative: 4 dets, one per tile, wrong coords.
    Retest baseline CONFIRMED HALLUCINATION (2026-04-10) — different failure mode triggered
                 by prompt change ("expect 20 or more"):
                 72 degenerate bboxes with x1=x2 (width=0, height=672-770 px) — vertical line
                 segments spanning image height, not column boxes. Only 2 distinct x positions
                 (x=38 and x=136). Model is outputting wall/grid line x-coordinates as columns.
    Episodic memory tests (2026-04-10, 1024px input):
                 Memory-ANCHOR (5 GT boxes as visual anchors):
                   5 echoed exactly, then 18 more generated in the same left x-strip (x~51-94).
                   Box sizes 32-41 px wide, 29-43 px tall — CORRECT size range.
                   gemma3:4b is the only model so far to extend anchors to NEW y-positions in
                   the same column strip rather than pure echo. More spatially aware than
                   qwen2.5vl and minicpm — but only found 1 of 5 column strips.
                 Memory-ADDONLY ("already found, find the others"):
                   25 detections — all "sliding echo": 5 slight x-offset variants per GT
                   example ([16,129,55,168], [36,129,80,168], [56,129,94,168]...). Generates
                   multiple overlapping copies shifted by ~10-20 px rather than new positions.
                 Conclusion: strongest memory-injection response seen — anchor phase shows
                 genuine spatial extension (not pure echo). But model cannot generalise to the
                 other 4 column strips; fine-tuning required for reliable detection.
    Iterative memory test — 4 rounds (2026-04-10):
                 Seed: 5 GT examples, one per x-strip (Row 1), all 5 columns seeded.
                 Round 1: found rows 3-4-5 of col 1 only  →  8/25 cells covered
                 Round 2: found row 2 of col 1; near-row-1 for cols 2-5 (y~62, false)  →  9/25
                 Round 3: plateau — 15 raw dets, all invalid (degenerate bboxes)  →  9/25
                 Round 4: plateau — identical to round 3  →  9/25
                 Final grid (X=covered, .=missing):
                   row1: X X X X X   (seed + round 2 near-border)
                   row2: X . . . .
                   row3: X . . . .
                   row4: X . . . .
                   row5: X . . . .
                 Findings:
                   - Model consistently explores DOWN its first anchor column (col1)
                     rather than generalising ACROSS to other column strips (cols 2-5),
                     even when all 5 strips are seeded.
                   - Memory context beyond ~20 entries causes reversion to degenerate
                     zero-width bboxes — context overload collapses the output quality.
                   - Maximum coverage: 9/25 cells (36%); cols 2-5 beyond row1 never found.
                   - Episodic memory alone cannot bootstrap full detection.
                     Fine-tuning on annotated floor plan data is the required next step.

[6] llama3.2-vision:11b      ~7.9 GB    Score: 2/6   FAIL   * STRONGEST ARCHITECTURE  * MEMORY INJECTION TESTED
    Detections : 4 after NMS
    Inference  : >300s/tile (2 tiles timed out)
    JSON rate  : 50% (2/4 tiles within timeout)
    Failure    : VRAM bottleneck — 7.9 GB model on 6.5 GB free VRAM causes heavy CPU
                 offload, inference exceeds 5 min/tile. Only 2 of 4 tiles completed.
                 The 4 returned detections have wrong bounding box positions.
    NOTE       : Architecturally the strongest model tested. On a 24 GB+ VRAM GPU or
                 via API, this model would likely perform significantly better.

    ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
    Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
      Phase        dets   TP   FP   FN    P      R     F1    elapsed
      BASELINE        0    0    0   86  0.000  0.000  0.000   137s
      ANCHOR          6    6    0   80  1.000  0.070  0.130    98s
      ADD-ONLY       10    0   10   86  0.000  0.000  0.000   324s
    Verdict: HELPS  (ANCHOR F1=0.130 vs BASELINE F1=0.000)
    Findings:
      - Baseline: 0 detections — model refuses to commit zero-shot. Consistent with original
        run failure (VRAM bottleneck prevented output on most tiles).
      - ANCHOR: 6 detections, 6 TP, 0 FP — PERFECT PRECISION. With 10 GT examples provided,
        model returns only locations it is highly confident about; every detection is correct.
        Model appears to validate each candidate against the anchor positions before committing.
        100% precision but only 7% recall — echoing confirmed locations rather than finding new.
      - ADD-ONLY: 324s, 10 detections, 0 TP. Given a larger pool, model generates 10 new
        guesses that miss all GT columns. Extended context may confuse spatial reasoning.
      - Conclusion: llama3.2-vision:11b shows the best ANCHOR precision of all models tested
        (P=1.00, 6 TP, 0 FP). On a 24 GB+ GPU where it runs at full speed, iterative ANCHOR
        injection rounds could systematically build up coverage. Currently VRAM-bottlenecked.

[7] qwen2.5vl:3b             ~3.2 GB    Score: 2/6   FAIL   * MEMORY INJECTION TESTED
    Detections : 1 after NMS
    Inference  : ~87s/tile (partly CPU-bound)
    JSON rate  : 100%
    Failure    : Almost no detections (count=1). 3B model is too small to confidently
                 identify column symbols. Tiles 2-4 return empty columns list despite
                 valid JSON structure. Single detection has wrong coordinates.

    ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
    Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
      Phase        dets   TP   FP   FN    P      R     F1    elapsed
      BASELINE        0    0    0   86  0.000  0.000  0.000   372s
      ANCHOR          7    7    0   79  1.000  0.081  0.151   185s
      ADD-ONLY        7    7    0   79  1.000  0.081  0.151   190s
    Verdict: HELPS  (ANCHOR+ADD-ONLY F1=0.151 vs BASELINE F1=0.000)
    Findings:
      - Baseline: 0 detections — model is over-conservative zero-shot (consistent with
        original run count=1). Very slow at 372s despite being 3B parameters.
      - ANCHOR: 7 TP, 0 FP — PERFECT PRECISION, stable at F1=0.151. Seeding activates
        detection; model returns a high-confidence subset of real column locations.
      - ADD-ONLY: identically 7 TP, 0 FP — the model returns the SAME 7 detections
        as the ANCHOR phase. It is not exploring new locations; it has memorised its
        ANCHOR output and repeats it. Consistent but not progressive.
      - Conclusion: qwen2.5vl:3b and llama3.2-vision:11b share a striking pattern — both
        achieve perfect precision (P=1.00) under ANCHOR injection while maintaining 0 FP.
        Both are echo-based: the 7 TP are likely a subset of the 10 seeded GT anchors.
        Without iterative re-anchoring with the full GT set, coverage cannot grow beyond ~8%.

[8] qwen3-vl:4b              ~3.5 GB    Score: 2/6   FAIL
    Detections : 1 after NMS  (original)  |  Retest: 0 across all configurations
    Inference  : ~27s/tile (original)  |  58-131s (retest — thinking overhead)
    JSON rate  : 100% (original)  |  0% (retest — all token budgets exhausted by thinking)
    Failure    : Original: same pattern as qwen2.5vl:3b — overly cautious, returns empty list.
    Retest CONFIRMED non-functional (2026-04-10):
                 Tested 4 configurations — 0 output in all cases:
                   baseline num_predict=8192: 33,544 chars internal thinking → 0 chars output
                   baseline think=False:      16,448 chars thinking → 0 chars output
                   memory-anchor (8192):      25,772 chars thinking → 0 chars output
                   memory-addonly (8192):     29,257 chars thinking → 0 chars output
                 think=False halved thinking verbosity but did NOT suppress it —
                 qwen3-vl-thinking renderer runs a structural chain-of-thought that cannot
                 be disabled via Ollama options. Model exhausts every token budget before
                 emitting a single character of actual response.
                 Memory injection: not testable — model never reaches output stage.
                 Conclusion: qwen3-vl:4b is non-functional for structured JSON detection
                 tasks under any Ollama parameter configuration tested.

--- TEXT-ONLY IMPOSTORS — no vision encoder; 2/6 score is a rubric artefact, not real capability ---

[9] ministral-3:3b           ~3.0 GB    Score: 2/6   FAIL   (NO VISION ENCODER)
    Detections : 86 (54 columns + 31 grid lines + 1 other)
    Inference  : 65s total
    JSON rate  : 100% (hallucinated — no image analysis)
    Failure    : Pure text LLM, no vision encoder. Ignores the image and hallucinates a
                 plausible-looking JSON array. Coordinates follow a regular 30-50 px stepped
                 pattern from top-left ([100,100,130,150], [150,100,180,140]...) — fabrication.
    NOTE       : Looks deceptively good (valid JSON, plausible count) but is entirely
                 hallucinated. Would silently produce meaningless results with no error signal.

[10] ministral-3:8b          ~6.0 GB    Score: 2/6   FAIL   (NO VISION ENCODER)
     Detections : 85 (0 columns + 85 grid lines)
     Inference  : 186s total
     JSON rate  : 100% (hallucinated — no image analysis)
     Failure    : Same as :3b — pure text LLM, no vision. Hallucinated 85 perfectly-spaced
                  vertical grid lines at x=100,200,300... with confidence=1.0 on every item.
                  Several coordinates exceed the 1280 px image width (x=1400-1800+).
                  Uniform confidence=1.0 is a hallucination signal — real models vary.
     NOTE       : Worse than :3b (zero columns detected vs 54). Both are unsuitable.

[30] ministral-3:latest      ~6.0 GB    Score: 2/6   FAIL   (NO VISION ENCODER)
     Detections : 79 (65 columns + 14 grid lines)
     Inference  : 128s total
     JSON rate  : 100% (hallucinated — no image analysis)
     Failure    : Despite `ollama show` listing "vision" capability, behaves identically
                  to :3b and :8b — ignores the image and hallucinates structured JSON.
                  Confidence=1.0 on all 79 items; coordinates follow the same regular
                  stepped pattern (250 px grid_line intervals, 30 px column intervals).
     NOTE       : ministral-3:latest is 8.9B parameters, 6.0 GB. Likely the same weights
                  as ministral-3:8b (same size); digest comparison not confirmed as :8b
                  was unloaded before cross-check. Both produce identical hallucination
                  signatures — treat as the same model family.


================================================================================
  TIER 3 — Score 1/6
  Valid JSON only — zero or near-zero usable detections
================================================================================

[11] glm-ocr:latest          ~2.2 GB    Score: 1/6   FAIL
     Detections : 2 (original)  |  Retest: 0 usable across all configurations
     JSON rate  : 100% (original)  |  Crash with full-length prompts; works only with short prompts
     Failure    : GLM-4V OCR model — treats prompt text as document content to extract/reproduce.
                  Not doing visual spatial reasoning; never locates anything in the image.
     GGML crash note: full-length detection prompts (400+ chars) + image → GGML tensor assertion
                  crash (same error as q8_0). Only short prompts (<100 chars) avoid the crash.
                  Root cause: long prompt context + image tiling exceeds tensor shape expectation.
    Episodic memory tests (2026-04-10, 1280px input, short prompt):
                 Baseline: echoed the prompt JSON template verbatim as output text — copied
                   "bbox":[x1,y1,x2,y2] literally. Zero spatial detection; treats schema as OCR.
                 Memory-ANCHOR: responded in plain English prose (not JSON), listing the
                   injected GT coords as a numbered text list. Reads examples as document
                   content, not as visual anchors.
                 Memory-ADDONLY: returned partial JSON array of injected GT examples with one
                   malformed entry (merged GT[3] y2=952 with GT[4] y2=1216 → 300px-tall box).
                 Conclusion: memory injection makes behaviour WORSE — injected coordinates
                   become "document text" for the OCR engine to reproduce. Model has NO
                   spatial detection capability and cannot be improved via prompt engineering.

[11=] glm-ocr:bf16             ~2.2 GB    Score: 1/6   FAIL   (ALIAS of glm-ocr:latest)
      Detections : 2
      JSON rate  : 100%
      Result     : Identical to glm-ocr:latest — same Ollama digest (6effedd0dc8a), same 1.1B
                   F16 weights. Memory injection test run on :bf16 tag — behaviour confirmed
                   identical (see [11] above).
      NOTE       : Skip-list entry "bf16 unquantized — likely >16 GB" was incorrect.
                   Model is F16 on 1.1B parameters = 2.2 GB, not >16 GB.

[12] moondream:latest        ~1.7 GB    Score: 1/6   FAIL   * MEMORY INJECTION TESTED
     Detections : 0
     JSON rate  : 100%
     Failure    : Returns {"columns": []} on every tile. Tiny general-purpose VLM not
                  designed for structured detection tasks requiring spatial JSON bbox output.

     ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
     Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
       Phase        dets   TP   FP   FN    P      R     F1    elapsed
       BASELINE        2    0    2   86  0.000  0.000  0.000    12s
       ANCHOR         85    0   85   86  0.000  0.000  0.000    13s
       ADD-ONLY      136    0  136   86  0.000  0.000  0.000    23s
     Verdict: no change  (F1=0.000 in all phases; FP count increases)
     Findings:
       - Baseline: 2 detections, 0 TP. Model generates minimal output zero-shot.
       - ANCHOR: 85 FP — anchor examples cause massive hallucination flood. Model
         treats the seeded boxes as a licence to generate detections everywhere.
       - ADD-ONLY: 136 FP — escalates further. The larger pool triggers even more output.
       - Conclusion: memory injection transforms moondream from "near-silent" to
         "hallucination flood" with no TP gain. Completely ineffective; model has no
         spatial grounding capability for 40-50 px architectural symbols.

[13] qwen3.5:2b              ~2.7 GB    Score: 1/6   FAIL   (TEXT MODEL — no vision)
     Detections : 0
     JSON rate  : 100% (text-only output)
     Failure    : TEXT language model, not a vision model. Accepts image payload without
                  error but ignores it entirely. Returns {"columns": []} with no visual processing.


================================================================================
  TIER 4 — Score 0/6
  No usable output — crashed, timed out, wrong output, or text-only
================================================================================

--- Ran but produced zero or invalid detections ---

[14] llava-phi3:3.8b         ~2.9 GB    Score: 0/6   FAIL   * MEMORY INJECTION TESTED
     Detections : 2 — both verbatim copies of the prompt example coordinates
     Inference  : ~32s/tile
     JSON rate  : 100%
     Failure    : Returns exact coordinates from the detection prompt example:
                    column    [120,340,155,375]  conf=0.92  (copied from prompt)
                    grid_line [0,500,1280,500]   conf=0.85  (copied from prompt)
                  Pattern-matched the prompt example rather than analysing the image.
     NOTE       : CLIP crash (Ollama 0.18.x) is FIXED in 0.20.4 for this variant.
                  The full LLaVA 7B CLIP crash persists in 0.20.4 (see [25]).

     ── MEMORY INJECTION TEST (2026-04-13, GT=86) ───────────────────────────────────────────
     Tile: 1024×1024 px.  Phases: BASELINE → ANCHOR (10 GT boxes, "find remaining") → ADD-ONLY.
       Phase        dets   TP   FP   FN    P      R     F1    elapsed
       BASELINE       55    0   55   86  0.000  0.000  0.000    44s
       ANCHOR         39    1   38   85  0.026  0.012  0.016    30s
       ADD-ONLY       54    0   54   86  0.000  0.000  0.000    22s
     Verdict: no change  (ANCHOR F1=0.016 < 0.02 threshold; baseline F1=0.000)
     Findings:
       - Baseline: 55 FP hallucinations, 0 TP. Dense false detections across the image.
       - ANCHOR: 1 TP out of 39 dets — marginal improvement but within noise threshold.
         The anchor constraint slightly reduces output count (55→39) but 38 remain FP.
       - ADD-ONLY: back to 54 FP, 0 TP. No stable benefit from injection.
       - Conclusion: memory injection has no meaningful effect. llava-phi3 lacks the
         spatial reasoning to use seeded examples as genuine visual anchors.

[15] qwen3.5:9b              ~6.6 GB    Score: 0/6   FAIL
     Detections : 0
     Inference  : 137-451s/tile
     Failure    : Hybrid vision+thinking model. CAN see the image — internal thinking field
                  (6,966 chars) correctly identifies floor plan content ("Grid Lines: Dashed
                  lines labeled with codes like RCB3, RCB2, SB1") — but returns empty content
                  (0 chars) on every attempt. All tokens consumed by chain-of-thought reasoning.
     Forced JSON tests (both failed):
       Tool calling  — 526s, 16,620 thinking chars, tool NEVER called.
       format=schema — schema respected but model used empty-array escape hatch.
     NOTE        : CAN see the floor plan but reasoning mode refuses to emit bbox
                   coordinates it cannot verify precisely. Fine-tuning required.

[16] qwen3.5:4b             ~3.5 GB    Score: 0/6   FAIL
     Detections : 0  (all configs tested)
     Renderer   : qwen3.5 (plain — NOT qwen3-vl-thinking)
     Failure    : Thinking model — inference budget consumed silently; NO output of any kind.
    Retest confirmed non-functional (2026-04-10, 4 phases):
                 All phases: 0 raw chars, 0 thinking chars, 0 detections.
                 Elapsed 56-78s per phase — model IS processing but emits nothing.
                 think=False reduced time 76s→56s (flag has effect) but still 0 output.
                 Unlike qwen3-vl:4b (which surfaces 16-33K thinking chars), qwen3.5:4b
                 runs in total silence — no thinking, no response, just consumed tokens.
                 Most likely text-only (no vision encoder); image ignored; reasoning loop
                 exhausts num_predict budget without emitting a single character.
                 Memory injection: not testable — model never produces any output.

[17] qwen3.5:0.8b           ~0.7 GB    Score: 0/6   FAIL
     Detections : 0
     Failure    : Thinking model — same behaviour as :9b / :4b. Even the smallest qwen3.5
                  variant refuses to emit coordinates it cannot verify.

[31] qwen3-vl:2b             ~1.9 GB    Score: 0/6   FAIL
     Detections : 0
     Inference  : 46s
     JSON rate  : 0% (empty output)
     Failure    : Thinking+vision hybrid. Produced no visible output — entire token budget
                  consumed by internal chain-of-thought reasoning with nothing emitted to the
                  response field. Same failure mode as qwen3.5:9b [15]. Despite being the
                  smallest qwen3-vl, the thinking layer overrides the vision output path.

[32] qwen3-vl:8b             ~6.1 GB    Score: 0/6   FAIL
     Detections : 0
     Inference  : 180s
     JSON rate  : 0% (empty output)
     Failure    : Same thinking+vision failure as qwen3-vl:2b [31] — no output emitted.
                  180s inference suggests the model ran and thought extensively but never
                  produced a final response.

[32=] qwen3-vl:latest        ~6.1 GB    Score: 0/6   FAIL   (ALIAS of qwen3-vl:8b)
      Detections : 0
      Inference  : 188s
      Result     : Same Ollama digest as qwen3-vl:8b (901cae732162) — identical model.
                   Not re-run; produces the same empty thinking-mode output.

[18] translategemma            —        Score: 0/6   FAIL   (NO VISION ENCODER)
     Detections : 0
     Failure    : Translation-focused model with no vision capability. Ignores image content.

[19] devstral                  —        Score: 0/6   FAIL   (NO VISION ENCODER)
     Detections : 0
     Failure    : Mistral code generation model. No vision encoder. Ignores image content.

[20] deepseek-ocr:3b         ~2.0 GB    Score: 0/6   FAIL
     Detections : 0
     Inference  : ~0s/tile (instant — no image processing)
     JSON rate  : 0%
     Failure    : Text-OCR model designed to extract text characters from documents. Does
                  not perform spatial object detection. Returns instantly with no bounding boxes.

--- Timeout / VRAM overflow ---

[21] ministral-3:14b         ~9.1 GB    Score: 0/6   TIMEOUT
     Detections : 0
     Inference  : 676s (>11 min — hard 300s limit exceeded)
     Failure    : 9.1 GB on 8 GB VRAM — massive CPU offload. Never produced output within
                  the timeout window. Whether it can detect columns is unknown.
     NOTE       : Tag WAS incorrectly listed as "does not exist on Ollama" — it does exist.
                  On a 24 GB+ VRAM GPU this would likely finish in 60-120s.

[22] mistral-small3.1:latest ~15.0 GB   Score: 0/6   TIMEOUT
     Detections : 0
     Inference  : 987–1686s total wall time across two runs (300s timeout fired; thread ran to completion)
     Failure    : 15 GB on 8 GB VRAM — heaviest CPU offload of all tested models.
     NOTE       : utils.py ThreadPoolExecutor waits for thread completion after firing timeout
                  (executor.shutdown(wait=True)), so reported time is true wall time, not the 300s limit.
                  Two runs were made: first 987s (single load), second 1686s (concurrent model load contention).
                  ollama show confirms HAS vision capability (contradicts earlier skip-list note).
                  Requires 24 GB+ VRAM.

[22=] mistral-small3.1:24b   ~15.0 GB    Score: 0/6   TIMEOUT  (ALIAS of mistral-small3.1:latest)
      Detections : 0
      Result     : Identical to mistral-small3.1:latest — same Ollama digest (b9aaf0c2586a),
                   same 15 GB weights. Not re-run; would produce the same 987–1686s timeout.

[23] llama4:16x17b           ~67.0 GB   Score: 0/6   OOM
     Detections : 0
     Inference  : 4s (load attempt only — RAM exhausted before first token)
     Failure    : System RAM exhausted. Ollama: "model requires 58.2 GiB, only 33.0 GiB
                  available." Full MoE weight set requires ~58 GB RAM. Cannot be fixed with
                  more VRAM alone — needs a machine with >=64 GB RAM.

--- CLIP runner crashes ---

[24] bakllava:7b             ~4.7 GB    Score: 0/6   CRASH (CLIP)
     Detections : 0
     Failure    : HTTP 500 on every image request. BakLLaVA uses the legacy LLaVA-1.5
                  CLIP-based vision encoder. Root cause: CLIP runner EOF crash (Ollama 0.18 bug).

[25] llava:7b                ~4.7 GB    Score: 0/6   CRASH (CLIP)
     Detections : 0
     Failure    : HTTP 500 on every tile, even with 64x64 px test images. Retested on
                  Ollama 0.20.4 — crash persists (44s then HTTP 500). Unlike llava-phi3:3.8b,
                  the full LLaVA 7B CLIP crash is NOT fixed in 0.20.4.

[26] llava-llama3:8b         ~5.5 GB    Score: 0/6   CRASH (CLIP)
     Detections : 0
     Failure    : HTTP 500 — same legacy CLIP runner crash as llava:7b and bakllava:7b.

--- GGML tensor crash ---

[27] glm-ocr:q8_0            ~1.6 GB    Score: 0/6   CRASH (GGML)
     Detections : 0
     Failure    : GGML tensor assertion crash on image input.
                  Error: "GGML_ASSERT(a->ne[2] * 4 == b->ne[0]) failed" (HTTP 500).
                  Q8_0 quantization changes tensor dimensions in a way that violates the
                  GGML attention kernel's shape assertion for the vision encoder.
                  Default glm-ocr:latest (F16, 2.2 GB) runs fine — only Q8_0 crashes.


================================================================================
  MEMORY INJECTION CROSS-MODEL SUMMARY (2026-04-13, GT=86 columns)
================================================================================

  14 models tested across all injection rounds (Round 12-13).
  Method: BASELINE → ANCHOR (10 spread GT boxes, "find remaining") → ADD-ONLY (pool).

  Model                    B-F1   A-F1   D-F1   Verdict   Notes
  ──────────────────────────────────────────────────────────────────────────────
  gemma4:e4b              0.050  0.050  0.000   HURTS     harness also tested; all phases bad
  gemma3:12b              0.115  0.000  0.021   HURTS     ANCHOR kills all output (0 dets, 336s)
  qwen2.5vl:7b            0.000   echo   echo   no change echo — returns injected examples verbatim
  gemma4:e2b              0.000  0.149  0.000   HELPS*    ANCHOR P=0.875; ADD-ONLY collapses to 0
  minicpm-v:8b            0.000   echo  0.000   no change echo → add-only finds wrong positions
  granite3.2-vision:2b    0.000  0.052  0.000   HELPS*    ANCHOR triggers flood (147 dets, 141 FP)
  llama3.2-vision:11b     0.000  0.130  0.000   HELPS*    ANCHOR P=1.000, 6 TP, 0 FP — best precision
  qwen2.5vl:3b            0.000  0.151  0.151   HELPS*    ANCHOR P=1.000, 7 TP; ADD-ONLY stable echo
  moondream:latest        0.000  0.000  0.000   no change ANCHOR triggers hallucination flood (85 FP)
  llava-phi3:3.8b         0.000  0.016  0.000   no change marginal, within noise
  gemma3:4b               0.000   —      —      no change iterative test; single-strip echo only
  qwen3-vl:4b             0.000   —      —      no change thinking model; never produces output
  glm-ocr:latest          0.000   —      —      no change OCR model; treats coords as document text
  qwen3.5:4b              0.000   —      —      no change thinking model; zero output all configs

  * "HELPS" reflects the ≥0.02 F1 delta rule. In practice, improvement is anchor-echo driven
    (models confirm the 10 seeded positions rather than finding new columns). True new-discovery
    rate remains near zero across all models.

  Key cross-model findings:
  1. ANCHOR phase consistently outperforms BASELINE and ADD-ONLY across all testable models.
     Providing 10 real GT positions is the strongest injection signal tested.
  2. Models with nonzero baseline (gemma3:12b, gemma4:e4b) are HURT — anchor prompt
     over-constrains them and collapses output.
  3. Models with zero baseline are "HELPED" — but only because they echo the anchors
     rather than discovering new columns. Peak recall capped at ~8-17% (7-15 of 86).
  4. ADD-ONLY phase is universally unreliable: gemma4:e2b, llama3.2-vision:11b, and
     granite3.2-vision:2b all collapse to 0 TP; qwen2.5vl:3b repeats its ANCHOR output.
  5. llama3.2-vision:11b and qwen2.5vl:3b are the only models to achieve perfect precision
     (P=1.00) under ANCHOR injection. Both are conservative: 6-7 detections, all correct,
     but confined to the seeded region. Most promising for iterative multi-round injection
     on a higher-VRAM machine.

  CONCLUSION: Text-based memory injection cannot compensate for the lack of fine-tuning.
  At best, ANCHOR injection provides 7-15 TP at perfect precision — useful as a sanity
  check but insufficient for production detection of 86 columns. YOLO remains the only
  viable approach on this hardware.


================================================================================
  ROOT CAUSE — WHY ALL LOCAL MODELS FAIL
================================================================================

  The core issue is localization precision, not visual understanding.

  1. COLUMN SIZE vs IMAGE SIZE
     True column symbols are 40-50 px in a 1285x1273 px page image.
     After resizing to 1024 px for the model, each column is ~32-40 px wide
     (~3% of image width). General-purpose VLMs are not trained to localize
     such small objects with sub-pixel precision.

  2. COARSE vs TIGHT BOUNDING BOXES
     Models output 100-300 px approximate boxes around structural regions.
     Ground truth boxes are 40-50 px tight around the actual column symbol.
     Overlap (IoU) between coarse model boxes and tight GT boxes is effectively
     zero — even if the model is looking at the right area.

  3. DOMAIN GAP
     VLMs are trained on natural images and general documents. They understand
     that this is a floor plan, but cannot distinguish between column symbols,
     grid balloons, wall intersections, and door arcs at the required precision.

  4. NO FINE-TUNING
     All models tested are zero-shot. Without training on architectural drawings,
     even the 11B LLaMA model fails.


================================================================================
  RECOMMENDED PATH FORWARD
================================================================================

  OPTION A — GPT-4o via API  (agent.py — already implemented)
    Cost    : ~$0.15-0.20 per A0 PDF (35 tiles at ~$0.005/tile with few-shot images)
    Quality : Significantly better with reference column images (few-shot prompting)
    Action  : Top up OpenAI credits; then run test_detection.ipynb to get P/R/F1 numbers

  OPTION B — YOLO fine-tune  (already deployed in v3 — PRODUCTION CHOICE)
    Speed   : ~50ms per page (1000x faster than any VLM tested)
    Quality : Significantly better P/R/F1 than SEA-LION baseline
    Weights : yolo_detection_agents/weights/column-detect.pt
    Status  : Active — this is the correct production approach

  OPTION C — gemma4:e2b fine-tuned
    Why     : Best structured output quality of all 27 models tested. Perfect JSON,
              correct types. Hallucinates coordinates zero-shot, but spatial grounding
              can be learned. Most promising candidate for floor-plan fine-tuning.
    Caveat  : 346s/tile on RTX 4000; needs 24 GB+ GPU or cloud API for practical use.

  OPTION D — llama3.2-vision:11b on better GPU
    Requirement : 24 GB+ VRAM (RTX 3090, A100)
    Why         : Architecturally the strongest model tested; currently VRAM-bottlenecked.


================================================================================
  ENVIRONMENT
================================================================================
  OS      : Linux (Ubuntu)
  Ollama  : 0.18.3 → upgraded to 0.20.4 for Rounds 3+ (gemma4, llava-phi3 retest)
  GPU     : NVIDIA Quadro RTX 4000  (8 GB VRAM, ~6.5 GB available during tests)
  RAM     : ~33 GB (Ollama reported 33.0 GiB available)
  Python  : 3.12 (anaconda yolo env)
  Tested  : 2026-04-07 to 2026-04-13
  Rounds  : 13
  Log     : pdf_detection_agent/benchmark/vision_model_evaluation_log.json

  CONCLUSION: Zero-shot VLMs cannot localize 40-50 px column symbols in CAD floor plans.
  Use YOLO (column-detect.pt) for production. Use OpenAI GPT-4o for cloud-based refinement.


================================================================================
  NVIDIA EDGE DEVICE ANALYSIS — SHOULD YOU BUY ONE?
================================================================================

DEVICE COMPARISON (April 2026)
----------------------------------------------------------------------------------------------------
Device                     TOPS   GPU Cores  RAM       Power    Price (USD)  Price (SGD est.)
----------------------------------------------------------------------------------------------------
Jetson Orin Nano Super      67    1024 CUDA   8 GB      7-25W   $249         ~$350
                                  32 Tensor   shared
Jetson Orin NX 8GB          70    1024 CUDA   8 GB     10-25W   $399         ~$550
                                  32 Tensor   shared
Jetson Orin NX 16GB        100    1024 CUDA  16 GB     10-40W   $599         ~$830
                                  32 Tensor   shared
Jetson AGX Orin 32GB       200    2048 CUDA  32 GB     15-50W   $1,599       ~$2,200
                                  64 Tensor   shared
Jetson AGX Orin 64GB       275    2048 CUDA  64 GB     15-60W   $1,999       ~$2,750
                                  64 Tensor   shared
----------------------------------------------------------------------------------------------------
Sources: nvidia.com/jetson-orin, developer.nvidia.com/embedded/jetson-modules


VLM PERFORMANCE ON JETSON (official NVIDIA benchmarks, JetPack 6.2 Super Mode)
----------------------------------------------------------------------------------------------------
VLM Model              Orin Nano 8GB   Orin NX 8GB   Orin NX 16GB    Unit
----------------------------------------------------------------------------------------------------
VILA 1.5 3B              1.1 tok/s       1.0 tok/s     1.3 tok/s     tok/s
VILA 1.5 8B              0.8 tok/s       1.0 tok/s     1.0 tok/s     tok/s
LLAVA 1.6 7B             0.6 tok/s       1.2 tok/s     0.7 tok/s     tok/s
Qwen2 VL 2B              4.4 tok/s       4.8 tok/s     4.8 tok/s     tok/s
InternVL2.5 4B           5.1 tok/s       4.1 tok/s     4.4 tok/s     tok/s
PaliGemma2 3B           21.6 tok/s      23.9 tok/s    23.8 tok/s     tok/s
SmolVLM 2B              12.9 tok/s      14.4 tok/s    14.3 tok/s     tok/s
----------------------------------------------------------------------------------------------------
Source: developer.nvidia.com/blog/nvidia-jetpack-6-2-brings-super-mode


WHAT THESE NUMBERS MEAN FOR YOUR PIPELINE
----------------------------------------------------------------------------------------------------
Your pipeline processes 35 tiles per A2 floor plan PDF.
Each tile needs ~100-500 output tokens from the VLM.

  Model             tok/s on Nano  Tokens needed  Time/tile  35 tiles     Full PDF
  ─────────────────────────────────────────────────────────────────────────────────
  VILA 1.5 3B          1.1          ~300            273s     ~2.6 hours   UNUSABLE
  Qwen2 VL 2B          4.4          ~300             68s     ~40 min      VERY SLOW
  PaliGemma2 3B       21.6          ~300             14s     ~8 min       MARGINAL
  SmolVLM 2B          12.9          ~300             23s     ~14 min      SLOW
  ─────────────────────────────────────────────────────────────────────────────────
  YOLO (TensorRT)    100+ FPS       N/A             0.01s    ~0.35s       INSTANT

  Even the fastest VLM (PaliGemma2 at 21.6 tok/s) takes 8 minutes per PDF.
  YOLO does the same job in under 1 second.


YOLO ON JETSON (the only viable edge use case)
----------------------------------------------------------------------------------------------------
  Your column-detect.pt (YOLOv11, 5.6 MB) converts to TensorRT on Jetson:
    trtexec --onnx=column-detect.onnx --saveEngine=column-detect.engine

  Expected performance on Jetson Orin Nano:
    - YOLOv8n (similar size): ~250 FPS at 640x640 (NVIDIA official benchmark)
    - Your model (YOLOv11): ~100-200 FPS estimated
    - Full 35-tile PDF: < 1 second total

  This is 500x-10,000x faster than any VLM on the same hardware.


BUDGET BREAKDOWN (if you buy)
----------------------------------------------------------------------------------------------------
                          Orin Nano Super   Orin NX 16GB    AGX Orin 64GB
  Dev kit                 $249 / ~$350      $599 / ~$830    $1,999 / ~$2,750
  NVMe SSD 256GB          $40 / ~$55        $40 / ~$55       $40 / ~$55
  MicroSD 64GB            $12 / ~$17        $12 / ~$17       included
  Case/enclosure           $25 / ~$35        $40 / ~$55       included
  Power supply             included          included         included
  ─────────────────────────────────────────────────────────────────────────
  TOTAL                   ~$326 / ~$457     ~$691 / ~$957    ~$2,039 / ~$2,805


================================================================================
  EDGE DEVICE VERDICT
================================================================================

  ANSWER: NO — Do not buy an edge device for VLM-based detection.
          YES — Buy ONLY if deploying YOLO on-site.

  DETAILED REASONING:

  1. VLMs CANNOT DO THIS TASK ON EDGE
     This entire benchmark (27 models tested across 11 rounds) proved that no local
     vision model — not even on a workstation GPU — can reliably localize 40-50 px
     column symbols in architectural floor plans. Every model scored 0.000 P/R/F1.
     Running the same failing models on weaker edge hardware will not fix this.
     A Jetson cannot run what a Quadro RTX 4000 already fails at.

  2. VLMs ARE TOO SLOW ON EDGE FOR YOUR PIPELINE
     Even if a VLM could detect columns, at 1-5 tok/s on Jetson it takes
     40 minutes to 2.6 hours per PDF. Your pipeline needs < 30 seconds.

  3. YOLO IS THE CORRECT EDGE SOLUTION
     Your column-detect.pt runs at 100+ FPS on Jetson via TensorRT.
     A full 35-tile floor plan completes in < 1 second. This is the only
     viable approach for on-site edge deployment.

  4. IF YOU NEED ON-SITE DEPLOYMENT:
     Buy: Jetson Orin Nano Super ($249 / ~$350 SGD)
     Why: Your YOLO model is only 5.6 MB — the Nano is massively overpowered
          for this. 67 TOPS and 1024 CUDA cores for a 5.6 MB model is overkill.
          No need for Orin NX ($599) or AGX Orin ($1,999).
     How: Convert YOLO to TensorRT, deploy with a simple FastAPI server on Jetson.
          Grid detection (SEA-LION) stays on the cloud/workstation.
          Revit API stays on Windows.

  5. IF YOU NEED BETTER VLM ACCURACY:
     Use OpenAI GPT-4o via API ($0.15/PDF). It is 1000x better than any local
     model at this task, and costs less than the electricity to run a Jetson
     for the same number of PDFs.

  6. DO NOT BUY AGX ORIN ($1,999) FOR THIS PROJECT
     Even with 64 GB RAM and 275 TOPS, the VLM accuracy problem remains.
     The bottleneck is model capability (zero-shot VLMs cannot localize small
     CAD symbols), not compute power. Spending $2,000 on hardware will not
     fix a $0 model architecture problem.

  FINAL RECOMMENDATION:
  ─────────────────────────────────────────────────────────────────────────
  | Use case                    | Device              | Budget (SGD)     |
  |─────────────────────────────|─────────────────────|──────────────────|
  | On-site YOLO deployment     | Orin Nano Super     | ~$460            |
  | Better VLM accuracy         | OpenAI GPT-4o API   | ~$15/month       |
  | Development/training        | Keep current GPU    | $0               |
  | Everything at once          | Orin Nano + GPT-4o  | ~$475 + $15/mo   |
  ─────────────────────────────────────────────────────────────────────────

================================================================================
  APPENDIX — VISION MODELS RANKED: FULL SUMMARY TABLE
================================================================================

  32 unique models (plus 4 alias tags) tested across 11 rounds on RTX 4000 (8 GB VRAM).
  Zero models passed (threshold >=4/6 points).

  HW Issue? — "Yes" = failure attributable to RTX 4000 hardware constraints (VRAM/RAM).
               "No"  = failure due to model architecture, training domain, or runtime bug.
  Alias      — (=X) = identical Ollama digest; result is the same as the aliased entry.

  Rank  Model                      GB    Score  Dets  Status       HW?  Root Cause
  ──────────────────────────────────────────────────────────────────────────────────────────────
     1  gemma4:e4b                 9.6   3/6    73    FAIL          No   Hallucinated coords; 73 cols packed in single row
    1=  gemma4:latest              9.6   3/6    73    FAIL          No   Alias of :e4b (same digest)
     2  gemma3:12b                 8.1   3/6   26/30  FAIL          No   Hallucination confirmed (2 runs): fabricated uniform grids, different each time
     3  qwen2.5vl:7b               6.0   3/6    12    FAIL          No   Hallucination confirmed; episodic memory injection echoes examples, no new detections
     4  gemma4:e2b                 7.2   3/6     9    FAIL          No   Hallucinated even-spaced coords (x=200,300,400,500)
     5  minicpm-v:8b               5.5   3/6     9    FAIL          No   Hallucination confirmed; add-only memory injection finds new boxes (right size, wrong position)
     6  granite3.2-vision:2b       1.9   3/6     8    FAIL          No   Valid JSON; bounding box coords incorrect
     7  ministral-3:latest         6.0   2/6    79    FAIL          No   No vision encoder; text-only fake JSON
     8  ministral-3:3b             3.0   2/6    86    FAIL          No   No vision encoder; text-only fake JSON
     9  ministral-3:8b             6.0   2/6    85    FAIL          No   No vision encoder; text-only fake JSON
    10  gemma3:4b                  3.0   2/6     4    FAIL          No   Hallucination confirmed (zero-width line bboxes); anchor memory extends to new y-positions (best spatial response so far)
    11  llama3.2-vision:11b        7.9   2/6     4    FAIL         Yes   VRAM limit; 7.9 GB -> heavy CPU offload -> degraded quality
    12  qwen2.5vl:3b               3.2   2/6     1    FAIL          No   Only 1 detection; coords incorrect
    13  qwen3-vl:4b                3.5   2/6     1    FAIL          No   Thinking model; exhausts ALL token budgets (even 8192) on CoT — 0 output in all configs tested
    14  glm-ocr:latest             2.2   1/6     2    FAIL          No   OCR engine echoes prompt text; memory injection makes it worse (reproduces injected coords as prose)
   14=  glm-ocr:bf16               2.2   1/6     2    FAIL          No   Alias of :latest — memory injection confirmed identical behaviour
    15  moondream:latest           1.7   1/6     0    FAIL          No   Cannot produce structured JSON output
    16  qwen3.5:2b                 2.7   1/6     0    FAIL          No   Text-only model; no vision encoder
    17  llava-phi3:3.8b            2.9   0/6     2    FAIL          No   CLIP runner bug; echoes prompt (fixed in Ollama 0.20.4)
    18  qwen3.5:9b                 6.6   0/6     0    FAIL          No   Thinking+vision hybrid; empty output after chain-of-thought
    19  qwen3-vl:2b                1.9   0/6     0    FAIL          No   Thinking+vision hybrid; empty output after chain-of-thought
    20  qwen3-vl:8b                6.1   0/6     0    FAIL          No   Thinking+vision hybrid; empty output after chain-of-thought
   20=  qwen3-vl:latest            6.1   0/6     0    FAIL          No   Alias of :8b (same digest)
    21  qwen3.5:4b                ~3.5   0/6     0    FAIL          No   Silent token consumption — 0 raw + 0 thinking output across all configs; not testable for memory injection
    22  qwen3.5:0.8b              ~0.7   0/6     0    FAIL          No   Thinking+vision hybrid; empty output after chain-of-thought
    23  translategemma              —    0/6     0    FAIL          No   No vision capability; NLP translation model
    24  devstral                    —    0/6     0    FAIL          No   No vision capability; code generation model
    25  deepseek-ocr:3b            2.0   0/6     0    FAIL          No   OCR-tuned; not trained for spatial detection
    26  ministral-3:14b            9.1   0/6     0    TIMEOUT      Yes   VRAM limit; 9.1 GB -> OOM/swap -> 676s timeout
    27  mistral-small3.1:latest   15.0   0/6     0    TIMEOUT      Yes   VRAM limit; 15 GB far exceeds 8 GB -> 987-1686s timeout
   27=  mistral-small3.1:24b      15.0   0/6     0    TIMEOUT      Yes   Alias of :latest (same digest)
    28  llama4:16x17b             67.0   0/6     0    OOM          Yes   67 GB far exceeds 8 GB VRAM; immediate abort
    29  bakllava:7b                4.7   0/6     0    CRASH         No   CLIP runner bug; HTTP 500 (Ollama <=0.20.4)
    30  llava:7b                   4.7   0/6     0    CRASH         No   CLIP runner bug; HTTP 500 (unfixed in Ollama 0.20.4)
    31  llava-llama3:8b            5.5   0/6     0    CRASH         No   CLIP runner bug; HTTP 500 (Ollama <=0.20.4)
    32  glm-ocr:q8_0               1.6   0/6     0    CRASH         No   GGML tensor assertion; Q8_0 incompatible with vision encoder
  ──────────────────────────────────────────────────────────────────────────────────────────────

  Models passed      : 0 / 32   (32 unique + 4 alias tags)
  HW-based failures  : 5   (llama3.2-vision:11b, ministral-3:14b, mistral-small3.1:latest, :24b, llama4:16x17b)
  Model-based failures: 27  (all remaining entries)
  Baseline           : SEA-LION v4 4B (~3.3 GB) — P=0.254  R=0.728  F1=0.376
  Environment        : Ubuntu · Ollama 0.18.3->0.20.4 · NVIDIA Quadro RTX 4000 (8 GB VRAM) · 33 GB RAM

================================================================================
  MEMORY INJECTION RANKED TABLE
  Rank by best F1 achieved under any injection phase (GT=86 columns, tol=25 px)
  Standard protocol: BASELINE → ANCHOR (10 spread GT boxes) → ADD-ONLY (pool)
================================================================================

  Rank  Model                    GB     B-F1   Best-F1  Best Phase     P@Best  Verdict
  ──────────────────────────────────────────────────────────────────────────────────────────
   1    qwen2.5vl:3b             3.2   0.000    0.151   ANCHOR+ADD-ONLY  1.000   HELPS
   2    gemma4:e2b               7.2   0.000    0.149   ANCHOR           0.875   HELPS
   3    llama3.2-vision:11b      7.9   0.000    0.130   ANCHOR           1.000   HELPS
   4    gemma3:12b               8.1   0.115    0.115   BASELINE only    0.194   HURTS
   5    granite3.2-vision:2b     1.9   0.000    0.052   ANCHOR           0.041   HELPS†
   6    gemma4:e4b               9.6   0.050    0.050   BASELINE         —       HURTS
   7    gemma3:4b                3.0   0.000   ~0.000   ANCHOR (partial) —       no change‡
   8    minicpm-v:8b             5.5   0.000   ~0.000   ANCHOR (echo)    —       no change
   9    qwen2.5vl:7b             6.0   0.000   ~0.000   ANCHOR (echo)    —       no change
  10    llava-phi3:3.8b          2.9   0.000    0.016   ANCHOR           0.026   no change
  11    moondream:latest         1.7   0.000    0.000   —                —       no change§
  12    glm-ocr:latest           2.2   0.000    0.000   —                —       HURTS¶
  ──────────────────────────────────────────────────────────────────────────────────────────

  Not testable (separate reasons):
    qwen3-vl:4b    — thinking model; exhausts token budget before any output
    qwen3.5:4b     — thinking model; zero output across all configurations
    qwen3-vl:2b/8b — thinking model; same as :4b
    qwen3.5:9b/0.8b— thinking model; same failure mode

  Notes:
   † granite3.2-vision:2b "HELPS" is a metric artefact — ANCHOR triggers a hallucination
     flood (147 dets, 141 FP, P=0.041). Not genuinely useful.
   ‡ gemma3:4b tested with iterative protocol (not standard 3-phase); best outcome was
     6/25 grid cells covered, confined to one column strip. F1 not formally measured.
   § moondream ANCHOR triggers hallucination flood (0→85 FP); F1 stays 0.
   ¶ glm-ocr ANCHOR causes model to read injected coordinates as OCR document content
     and reproduce them as prose, not JSON detections.

  Key observations:
   1. Ranks 1-3 (qwen2.5vl:3b, gemma4:e2b, llama3.2-vision:11b) all achieve perfect or
      near-perfect precision (P=0.875-1.000) under ANCHOR injection. They are not
      discovering new columns — they are echoing the 10 seeded GT positions and returning
      a high-confidence subset. True new-discovery recall remains near zero.
   2. Ranks 1 and 3 (qwen2.5vl:3b, llama3.2-vision:11b) hit P=1.00 — every detection is
      a real column. On adequate hardware, iterative ANCHOR rounds could systematically
      grow coverage by re-seeding with confirmed detections each round.
   3. gemma3:12b (rank 4) is the ONLY model where the baseline outperforms all injection
      phases. Its zero-shot F1=0.115 (7 TP) is the highest unaided score across all models,
      but injection collapses output entirely.
   4. ADD-ONLY phase is universally unreliable — it either collapses to 0 (gemma4:e2b,
      llama3.2-vision:11b, granite3.2-vision:2b) or merely repeats the ANCHOR output
      (qwen2.5vl:3b). No model improved by seeing the accumulated pool.
   5. All "HELPS" verdicts are driven by ANCHOR providing 10 real GT positions.
      Without seeding, every model in this table produces F1=0.000 zero-shot.
      Memory injection is not a substitute for fine-tuning.


================================================================================
  SELF-EVOLUTION CANDIDATES — ITERATIVE MEMORY INJECTION TEST
  3 models × 5 rounds  |  Tested: 2026-04-13  |  GT=86 columns
================================================================================

  Selection criteria: P=1.000 under ANCHOR injection OR demonstrated spatial
  extension beyond seeded positions in episodic tests.

  Model                  GB    Why selected                                         Outcome
  ──────────────────────────────────────────────────────────────────────────────────────────────────────
  qwen2.5vl:3b           3.2   P=1.000 stable across ANCHOR+ADD-ONLY (7 TP, 0 FP)  INCONCLUSIVE — erratic across rounds; no self-evolution
  llama3.2-vision:11b    7.9   P=1.000 at ANCHOR (6 TP, 0 FP); strongest arch      INCONCLUSIVE — one strong round then collapses
  gemma3:4b              3.0   Extended beyond anchors (not pure echo, prior test)  MOST CONSISTENT — stable echo, but no self-evolution

  Test protocol (5 rounds per model):
    Round 1 : BASELINE — no injection, zero-shot detection
    Round 2 : ANCHOR   — 10 spread GT seeds injected, "find remaining"
    Round 3 : ANCHOR   — pool grows: seeds + Round 2 TPs
    Round 4 : ANCHOR   — pool grows: Round 3 pool + Round 3 TPs
    Round 5 : ANCHOR   — pool grows: Round 4 pool + Round 4 TPs
  Image legend: GREEN=GT(86)  CYAN=confirmed pool (prior rounds)  RED=current detections
  Script : benchmark/test_iterative_memory_injection.py
  Images : /tmp/mi_iter/<model>_r<N>.png
  Results: /tmp/mi_iter_results.json

  ── RESULTS ──────────────────────────────────────────────────────────────────────────────

  [A] qwen2.5vl:3b  (3.2 GB)
  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
    Round   Label                   Dets   TP   FP   FN    F1   Pool
    R1      BASELINE                  24    6   18   80  0.109     0
    R2      ANCHOR (seed=10)          10    0    0   86  0.000    10
    R3      ANCHOR (pool=10)           0    0    0   86  0.000    10
    R4      ANCHOR (pool=10)           7    7    0   79  0.151    10
    R5      ANCHOR (pool=10)           7    0    7   86  0.000    10
    Pool never grew beyond 10. No new columns discovered beyond seeds.

    Positive impact of memory injection?  NO
    - Baseline (R1) produced 6 TP zero-shot with no injection — better than R2/R3/R5.
    - Injection did NOT consistently improve detection. R4 achieved P=1.00 (7 TP) but
      R5 with identical prompt returned 7 FP. Temperature=0.1 failed to suppress variance.
    - Model is non-deterministic: same pool produces F1=0.151 one round and F1=0.000 the next.
    - Pool stayed locked at 10 seeds — model never ventured beyond injected positions.
    Verdict: MEMORY INJECTION DOES NOT HELP. Baseline is more reliable than any injected round.

  [B] llama3.2-vision:11b  (7.9 GB)
  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
    Round   Label                   Dets   TP   FP   FN    F1   Pool
    R1      BASELINE                   0    0    0   86  0.000     0
    R2      ANCHOR (seed=10)          10   10    0   76  0.208    10  ← best
    R3      ANCHOR (pool=10)          10    0   10   86  0.000    10
    R4      ANCHOR (pool=10)          20    0    0   86  0.000    10  †
    R5      ANCHOR (pool=10)           0    0    0   86  0.000    10
    Pool never grew beyond 10. No new columns discovered beyond seeds.
    † R4: 20 dets reported but 0 TP and 0 FP — all 20 had malformed bbox format (not parseable).

    Positive impact of memory injection?  PARTIALLY — R2 only
    - R2 is the single best result: 10 TP, 0 FP, P=1.000, F1=0.208 — the model returned
      exactly the 10 seeded positions with perfect precision. This is a genuine positive
      impact from injection, but it is an echo of the seeds, not new discovery.
    - R3 onward collapsed completely (0 TP, malformed output, empty responses).
      Likely VRAM pressure causing degradation as context length grows with pool text.
    - Injection activates detection (R1=0 dets → R2=10 TP) but cannot sustain it.
    Verdict: MEMORY INJECTION HELPS IN R2 ONLY. Not stable enough for iterative use
    on 8 GB VRAM. Requires 24 GB+ GPU for consistent multi-round performance.

  [C] gemma3:4b  (3.0 GB)
  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
    Round   Label                   Dets   TP   FP   FN    F1   Pool
    R1      BASELINE                   9    3    6   83  0.063     0
    R2      ANCHOR (seed=10)          10   10    0   76  0.208    10  ← stable from here
    R3      ANCHOR (pool=10)          10   10    0   76  0.208    10
    R4      ANCHOR (pool=10)          10   10    0   76  0.208    10
    R5      ANCHOR (pool=10)          10   10    0   76  0.208    10
    Pool never grew beyond 10. Model returned the same 10 seed positions every round.

    Positive impact of memory injection?  YES — but only as a precision booster
    - Injection transforms a noisy baseline (3 TP, 6 FP) into a clean, consistent
      detector (10 TP, 0 FP) for the seeded region. P jumps from 0.33 → 1.00.
    - F1 improves from 0.063 → 0.208 and holds perfectly across all 4 injected rounds.
    - Zero false positives in R2–R5: model is suppressing hallucination and confirming only
      what was given. This is the most reliable injection behaviour observed across all models.
    - However: the pool never grew. Model echoed the exact same 10 seeds every round.
      No new columns were discovered — coverage is capped at 10/86 (12%).
    Verdict: MEMORY INJECTION HELPS FOR PRECISION. It eliminates FP and stabilises output
    around the seeded positions. It DOES NOT help for recall — coverage cannot grow
    without a human providing additional verified seed positions each round.

  ── CROSS-MODEL VERDICT ──────────────────────────────────────────────────────────────────

    Model                  Injection positive impact?   Best F1   Notes
    qwen2.5vl:3b           NO                           0.151     Erratic; baseline (0.109) is safer
    llama3.2-vision:11b    PARTIALLY (R2 only)          0.208     Collapses after R2; VRAM-limited
    gemma3:4b              YES (precision only)         0.208     Stable P=1.00 but zero new discovery

    Critical finding: The pool NEVER grew beyond the initial 10 seeds across all 3 models
    and all 5 rounds. Memory injection is a confirmation mechanism, not a discovery engine.
    None of the 3 candidates can self-evolve — they can only validate what they are told.
    Coverage beyond the seeded positions requires either fine-tuning or human annotation.


================================================================================
END OF REPORT
================================================================================
